#include <string.h>
#include <stdio.h>

void main() {

	char ss[] = "XYZ";
	int len;

	len = strlen(ss); // 문자열 배열 ss의 길이를 구함 ( 널 문자 제외 )

	// 큰따옴표의 내용을 출력하기 위해 \" 문자를 사용함
	printf("문자열 \"%s\"의 길이 ==> %d \n", ss, len);
}
