#include <stdio.h>

void main() {
	
	int hap = 0;
	int i;

	for ( i = 1; 1 <= 100; i++ ) 
	{
		hap = hap + i;

		if ( hap >= 1000 ) // hap이 1,000보다 크거나 같으면 for 문을 빠져나감.
			break;
	}

	printf("1 ~ 100의 합에서 최초로 1000이 넘는 위치는 ? : %d\n", i);
}
